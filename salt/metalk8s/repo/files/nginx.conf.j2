server {
    listen       {{ listening_port }};
    server_name  localhost;

    location / {
        root   /var/www/repositories;
        autoindex on;
    }

    include conf.d/{{ nginx_registry_config }};
    include conf.d/{{ nginx_registry }};
}
